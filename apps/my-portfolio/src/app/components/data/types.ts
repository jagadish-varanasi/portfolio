export interface Article {
  id: string;
  title: string;
  description: string;
  topic: string;
  readDuration: string;
  date: string;
}

export interface Project {
  id: string;
  title: string;
  description: string;
  icon: string;
  url:string;
}
